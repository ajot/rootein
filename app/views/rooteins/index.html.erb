<div>
  <h1 class="font-bold text-4xl">Rooteins#index</h1>
  <p>You have <%= @rooteins.count %> rooteins.</p>
  <ol class="list-decimal list-inside">
  <% @rooteins.each do |rootein| %>
    <li><strong><%= rootein.name %></strong> — started <%= rootein.created_at.strftime("%B %d, %Y")
  %></li>
  <% end %>
  </ol>
</div>
